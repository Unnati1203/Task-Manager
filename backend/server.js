const path=require('path');require('dotenv').config({path:path.resolve(__dirname,'../.env')}); const express=require('express'),mongoose=require('mongoose'),cors=require('cors'),helmet=require('helmet'),rateLimit=require('express-rate-limit'),morgan=require('morgan');
const app=express(); app.use(helmet({crossOriginResourcePolicy:false}));app.use(cors({origin:process.env.CLIENT_URL||'http://localhost:5173'}));app.use(express.json({limit:'1mb'}));app.use(morgan('dev'));app.use('/uploads',express.static(path.resolve(process.env.UPLOAD_DIR||'uploads')));app.use('/api/auth',rateLimit({windowMs:15*60*1000,max:100}),require('./routes/auth'));app.use('/api',require('./routes/api'));app.use((e,req,res,next)=>{console.error(e);res.status(e.status||500).json({success:false,message:'Unable to complete request'})});
const port=process.env.PORT||5000;if(require.main===module)mongoose.connect(process.env.MONGO_URI).then(()=>app.listen(port,()=>console.log(`API running on ${port}`))).catch(e=>{console.error('MongoDB connection failed:',e.message);process.exit(1)}); module.exports=app;
